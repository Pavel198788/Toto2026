# Handoff — 2026-06-06

## Project
Тото 2026 — футбольный тотализатор ЧМ 2026. Репо: `/Users/nichegosebe/toto-2026/`

## What We Accomplished This Session
- Задеплоили сайт на Vercel: **https://toto-2026.vercel.app**
- Зарегистрировали аккаунт Vercel (Hobby, бесплатный), пользователь `pavel198788`
- Прописали все env vars на Vercel: DATABASE_URL, AUTH_SECRET, CRON_SECRET, FOOTBALL_DATA_API_KEY, NEXTAUTH_URL, GMAIL_USER, GMAIL_APP_PASSWORD
- Починили 3 проблемы деплоя: cron каждые 2 минуты (запрещён на Hobby), отсутствие `prisma generate` в build-скрипте, TypeScript ошибка `Stage` из `@prisma/client`
- Реализовали функцию «Забыли пароль?»: страницы `/auth/forgot-password` и `/auth/reset-password`, API-роуты, таблица `PasswordResetToken` в БД, отправка через Gmail SMTP (Nodemailer)
- Починили middleware (`proxy.ts`) — добавили forgot/reset-password в публичные пути

## Current State
Сайт работает на **https://toto-2026.vercel.app**. Функция «Забыли пароль?» задеплоена, страница открывается. **Отправка письма ещё не проверена** — в последнем деплое исправлен GMAIL_USER (был `p9269610811@gmail.com`, стал `p9610811@gmail.com`). Нужно проверить что письмо реально приходит.

Все изменения не закоммичены (uncommitted).

**Git state:**
- Branch: `main`
- Uncommitted changes: `.handoff.md`, `app/auth/login/page.tsx`, `package.json`, `package-lock.json`, `prisma/schema.prisma`, `proxy.ts`, `vercel.json` + новые файлы: `app/api/auth/forgot-password/`, `app/api/auth/reset-password/`, `app/auth/forgot-password/`, `app/auth/reset-password/`, `prisma/migrations/20260606120000_add_password_reset_token/`
- Last commit: `71f6a7e fix: Edge-safe auth split, proxy.ts, skip TBD teams in sync`

## Pending Tasks
1. [ ] **Проверить отправку письма** — зайти на https://toto-2026.vercel.app/auth/forgot-password, ввести email зарегистрированного пользователя, убедиться что письмо пришло и ссылка работает
2. [ ] **Закоммитить все изменения** — `git add -A && git commit -m "feat: deploy to Vercel, forgot-password flow via Gmail SMTP"`
3. [ ] **Зарегистрировать участников** — они сами регистрируются через `/auth/register` или ты регистрируешь за них
4. [ ] **Проверить кнопку ручной синхронизации** в админке (`/admin`) — должна обновлять счета матчей

## Blockers & Open Questions
- Письма отправляются через личный Gmail `p9610811@gmail.com` — Google может заблокировать отправку с нового IP (Vercel), если сработает защита аккаунта. Если письма не приходят — проверить папку "Спам" и входящие на `p9610811@gmail.com` на предмет уведомления о блокировке.

## Key Decisions Made
- **Gmail SMTP вместо Resend**: Resend бесплатный тариф позволяет слать письма только на верифицированный email владельца. Gmail через Nodemailer работает с любыми получателями.
- **Пароль приложения Gmail** (не основной пароль): создан на myaccount.google.com/apppasswords для аккаунта `p9610811@gmail.com`
- **Vercel Hobby план**: бесплатный, cron только раз в сутки (убрали cron каждые 2 минуты из vercel.json)
- **`prisma generate` в build**: добавлен в `package.json` скрипт `"build": "prisma generate && next build"` — без этого TypeScript не видит enum'ы Prisma на Vercel
- **`proxy.ts` публичные пути**: добавлены `/auth/forgot-password` и `/auth/reset-password` в `publicPaths` — иначе middleware редиректил на логин

## Context to Remember
- Пользователь не знаком с разработкой — нужны простые объяснения
- Язык общения: русский
- Vercel аккаунт: `pavel198788`, проект `toto2026/toto-2026`
- Admin аккаунт: `admin@toto2026.ru` / `admin123`
- Gmail для отправки: `p9610811@gmail.com` (пароль приложения хранится в Vercel env `GMAIL_APP_PASSWORD`)
- Neon PostgreSQL: `ep-red-scene-aqh8s2tq-pooler.c-8.us-east-1.aws.neon.tech`
- football-data.org бесплатный лимит: 10 запросов/минуту

## Starter Prompt for Next Session
> Продолжаем Тото 2026 (футбольный тотализатор ЧМ 2026). Репо: `/Users/nichegosebe/toto-2026/`, сайт: https://toto-2026.vercel.app. Читай `.handoff.md` для полного контекста. Нужно проверить что функция «Забыли пароль?» реально отправляет письма, и закоммитить все изменения.
