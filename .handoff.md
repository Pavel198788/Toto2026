# Handoff — 2026-06-12 (сессия 15)

## Project
Тото 2026 — футбольный тотализатор ЧМ-2026. Репо: `/Users/nichegosebe/toto-2026/`

## Что сделано в этой сессии

### 1. Клик на матч (РЕШЕНО — commit `6e7c108`)
Все предыдущие попытки были в рамках `"use client"` компонента (Link → router.push → plain `<a href>`). Радикальное решение: убрали `"use client"` из `components/matches-list.tsx` полностью. Компонент стал серверным — браузер получает чистый HTML `<a href>` без участия React hydration. Live-score polling (обновление каждые 5 мин) временно убран из списка, но страница `force-dynamic` и всегда показывает актуальные данные.

### 2. Профиль — блок "СРАВНЕНИЕ С ГРУППОЙ" (баги + замена)

**Найдены 2 бага в `lib/profile-stats.ts` → `calcComparison`:**
- `EXACT = new Set([11, 22, 32])` — неверные константы (должно быть [12, 25, 35]). Исправлено.
- `others.some(o => points > 0)` → "Все угадали кроме тебя" срабатывало при ОДНОМ угадавшем. Изменено на порог ≥60% других участников.
- Лейбл изменён на "Большинство угадали, ты нет".

**Блок заменён** на "НАСКОЛЬКО ТЫ УНИКАЛЕН" (commit `62d8a39`, `f8c2ec6`, `5c4076e`):
- Показывает % матчей где твой исход отличался от большинства группы
- "против большинства: X / Y" + "из них угадал: N"
- Архетип: Человек толпы / Умеренный / Самобытный / Бунтарь / Анархист
- Функция `calcUniqueness` в `lib/profile-stats.ts`

## Current State

- Branch: `main`, синхронизирован с `origin/main`
- Uncommitted: `.handoff.md` (этот файл)
- Last commit: `5c4076e` (fix uniqueness threshold)
- БД: 72 матча, 2 завершённых, 14 участников
- Vercel: работает, последний деплой ~5 мин назад

## Pending Tasks

1. [ ] **Подтвердить клик на матч** — убедиться что переход на `/matches/{id}` работает после серверного рефактора
2. [ ] **Live-score polling** — временно убран из списка матчей. При желании вернуть как отдельный клиентский компонент-оверлей поверх серверного списка
3. [ ] **Проверить начисление очков** за завершённые матчи — убедиться что у игроков появились очки в `/standings` или `/grid`
4. [ ] **Vercel cron лимиты** — Hobby plan ограничен 2 активными cron-заданиями из 7 в `vercel.json`

## Key Decisions Made

- **Server component для MatchesList**: убрали `"use client"` полностью — единственный способ гарантировать работу `<a href>` без зависимости от React hydration
- **Трейдоф**: live-score polling убран из списка. Это приемлемо — страница force-dynamic, данные свежие при загрузке
- **Блок "Насколько ты уникален"**: сравнивает исход пользователя с plurality (самым популярным исходом) среди остальных участников на каждом завершённом матче

## Context to Remember

- Vercel: `toto-2026.vercel.app`, Hobby план
- DB: `ep-red-scene-aqh8s2tq-pooler.c-8.us-east-1.aws.neon.tech`, password: `npg_VbaFPOI3iT4G`
- Admin: `admin@toto2026.ru` / `admin123`
- GitHub push: `env -u HTTPS_PROXY -u HTTP_PROXY git push origin main`
- Shell cwd сбрасывается на `/Users/nichegosebe/cruise-guide` — использовать абсолютные пути
- Prisma 7 требует PrismaPg adapter — для скриптов использовать паттерн из `/tmp/fix-duplicates2.mjs`
- 14 участников, 2 завершённых матча на 2026-06-12
- Scoring: GROUP exact=12, playoff exact=25, final exact=35; correct outcome = max(0, 10-goalError)
- Cron endpoint: `GET /api/cron/sync-scores?mode=all` с заголовком `Authorization: Bearer {CRON_SECRET}`
- Admin trigger sync: `POST /api/admin/trigger-sync` (требует admin-сессию)

## Starter Prompt для нового окна

> Продолжаем Тото 2026. Репо: `/Users/nichegosebe/toto-2026/`. Читай `.handoff.md`.
