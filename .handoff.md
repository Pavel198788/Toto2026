# Handoff — 2026-06-12 (сессия 16)

## Project
Тото 2026 — футбольный тотализатор ЧМ-2026. Репо: `/Users/nichegosebe/toto-2026/`

## What We Accomplished This Session

- **Клики на матчи** — `components/matches-list.tsx` переведён в серверный компонент (убрали `"use client"`). 70 `<a href>` тегов в HTML подтверждены curl-ом. Навигация на `/matches/{id}` работает.
- **Баги в profile-stats** — исправлена константа `EXACT: [11,22,32] → [12,25,35]` и порог "большинство угадали" (был `any` → стал `≥60%`)
- **Блок профиля заменён** — "СРАВНЕНИЕ С ГРУППОЙ" → "НАСКОЛЬКО ТЫ УНИКАЛЕН" (функция `calcUniqueness` в `lib/profile-stats.ts`)

## Current State

Код на диске и в git правильный. Проблема: Vercel показывал пользователю (Хлебников Павел) старый блок "СРАВНЕНИЕ С ГРУППОЙ" несмотря на пуши. Был сделан force-redeploy пустым коммитом `afbd860`. Локальный `next build` проходит чисто, `/profile` — dynamic. **Неизвестно подтвердил ли пользователь что новый блок появился** — это первое что нужно проверить.

**Git state:**
- Branch: `main`, синхронизирован с `origin/main`
- Uncommitted: ничего
- Last commit: `afbd860` (force redeploy)

## Pending Tasks

1. [ ] **Подтвердить что "НАСКОЛЬКО ТЫ УНИКАЛЕН" показывается** у Хлебникова Павла после `afbd860` деплоя. Если нет — смотреть Vercel Build Logs в Dashboard на предмет ошибок сборки.
2. [ ] **Подтвердить клики на матчи** — пользователь ещё не подтвердил что переход на `/matches/{id}` работает.
3. [ ] **Проверить начисление очков** за 2 завершённых матча — зайти на `/standings` или `/grid`.
4. [ ] **Vercel cron лимиты** — Hobby plan: 2 активных из 7 в `vercel.json`. Проверить в Dashboard.

## Blockers & Open Questions

- Vercel может не подхватывать деплои — если блок всё ещё старый, открыть Vercel Dashboard → Deployments и посмотреть статус последних билдов.
- Причина почему предыдущие деплои не применялись — неизвестна (возможно build cache или webhook issue).

## Key Decisions Made

- **Server component для MatchesList**: убрали `"use client"` — единственный надёжный способ без React hydration. Live-score polling убран (страница `force-dynamic`, данные свежие при загрузке).
- **calcUniqueness**: сравнивает исход пользователя с plurality (самым популярным) среди остальных. Блок показывается при `uniqueness.total > 0`.
- **Верификация через admin** — ошибка сессии: admin не имеет прогнозов, поэтому ни старый ни новый блок не показывались. Нужно проверять под реальным аккаунтом.

## Context to Remember

- Vercel: `toto-2026.vercel.app`, Hobby план
- DB: `ep-red-scene-aqh8s2tq-pooler.c-8.us-east-1.aws.neon.tech`, password: `npg_VbaFPOI3iT4G`
- Admin: `admin@toto2026.ru` / `admin123` (нет прогнозов — не использовать для проверки блоков профиля!)
- GitHub push: `env -u HTTPS_PROXY -u HTTP_PROXY git push origin main`
- Shell cwd сбрасывается на `/Users/nichegosebe/cruise-guide` — использовать абсолютные пути
- 14 участников, 2 завершённых матча на 2026-06-12
- Scoring: GROUP exact=12, playoff exact=25, final exact=35
- TypeScript ошибки в `.next/dev/types/validator.ts` — pre-existing, на production build не влияют

## Starter Prompt for Next Session

> Продолжаем Тото 2026. Репо: `/Users/nichegosebe/toto-2026/`. Читай `.handoff.md`. Задача 1: спросить у Хлебникова Павла — появился ли блок "НАСКОЛЬКО ТЫ УНИКАЛЕН" в профиле (был задеплоен `afbd860` force-redeploy). Если нет — открыть Vercel Dashboard и проверить статус последних деплоев.
