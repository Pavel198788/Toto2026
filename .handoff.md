# Handoff — 2026-06-06

## Project
Тото 2026 — футбольный тотализатор ЧМ 2026. Репо: `/Users/nichegosebe/toto-2026/`

## What We Accomplished This Session
- Task 2: Prisma схема + миграция (User, Match, Prediction, BonusPrediction, 3 энума). Prisma 7 использует `prisma.config.ts` вместо `url` в datasource — это нормально. BonusPrediction получил `@@unique([userId, type])`.
- Task 3: NextAuth v5 credentials auth, register endpoint, middleware. Добавлены security фиксы: timing-safe login (DUMMY_HASH), JSON parse guard, server-side password length ≥ 6.
- Task 4: Scoring logic TDD — `lib/scoring.ts` + 17 тестов Vitest, все проходят.
- Task 5: `lib/football-api.ts` — клиент football-data.org с mapStage/mapStatus.
- Task 6: 5 API роутов (predictions, leaderboard, grid, cron/sync-scores, admin/trigger-sync). Фиксы: timing-safe CRON_SECRET, winner field в upsert, JSON parse guard, score validation, error handling в trigger-sync.

## Current State
Backend-слой полностью реализован: схема, авторизация, scoring, API клиент, все API роуты. TypeScript компилируется без ошибок. Vitest тесты: 17/17 pass. Нет ни одного UI-файла — всё ещё стандартный `app/page.tsx` из create-next-app. Продолжение: Tasks 7–15 (UI, страницы, Vercel config).

PostgreSQL не был запущен во время разработки — миграция создана (файлы есть) но не применена к БД. Для запуска нужен docker или локальный PostgreSQL.

**Git state:**
- Branch: `main`
- Uncommitted changes: none
- Last commit: `50a1ecc fix: cron winner field, timing-safe secret compare, JSON error handling, score validation`

## Pending Tasks
1. [ ] **Task 7: Layout, Navbar, Providers** — `components/providers.tsx`, `components/navbar.tsx`, обновить `app/layout.tsx`, `app/globals.css`. Тёмная тема, навигация с auth state.
2. [ ] **Task 8: Страницы авторизации** — `app/auth/login/page.tsx`, `app/auth/register/page.tsx`.
3. [ ] **Task 9: Главная страница + match-card** — `app/page.tsx`, `components/match-card.tsx`.
4. [ ] **Task 10: Страница матчей + форма прогноза** — `app/matches/page.tsx`, `app/matches/[id]/page.tsx`, `components/prediction-form.tsx`.
5. [ ] **Task 11: Leaderboard** — `app/leaderboard/page.tsx`, `components/leaderboard-table.tsx`.
6. [ ] **Task 12: Grid** — `app/grid/page.tsx`, `components/predictions-grid.tsx`.
7. [ ] **Task 13: Profile** — `app/profile/page.tsx`.
8. [ ] **Task 14: Admin** — `app/admin/page.tsx`.
9. [ ] **Task 15: Vercel config + .env.example** — `vercel.json` (cron job), финальный `.env.example`.

## Blockers & Open Questions
- **PostgreSQL для применения миграции** — нужен для `npx prisma migrate dev`. Docker команда: `docker run -d --name toto-postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=toto2026 -p 5432:5432 postgres:16-alpine`. DATABASE_URL: `postgresql://postgres:postgres@localhost:5432/toto2026`
- **API ключ football-data.org** — нужен для реальных данных (Task 5 и cron). Регистрация: https://www.football-data.org/

## Key Decisions Made
- **Prisma 7**: datasource URL в `prisma.config.ts`, не в `schema.prisma` — это breaking change Prisma v7, нормально
- **Timing-safe auth**: DUMMY_HASH в `lib/auth.ts` для login; `timingSafeEqual` для CRON_SECRET в cron route
- **BonusPrediction `@@unique([userId, type])`**: добавлено vs оригинальный спек — предотвращает дублирование бонусных прогнозов
- **`winner` в cron upsert**: Prisma `update` блок включает `winner` (был баг — исправлен). winner = team name, берётся из `fdMatch.score.winner` ("HOME_TEAM"/"AWAY_TEAM" → team name)
- **`FDScore.winner`**: добавлено в интерфейс `lib/football-api.ts`
- **Субагент-ориентированная разработка**: `superpowers:subagent-driven-development` — один субагент на задачу, spec review + quality review после каждой

## Context to Remember
- Дизайн-спек: `docs/superpowers/specs/2026-06-06-toto-2026-design.md`
- План реализации (15 задач): `docs/superpowers/plans/2026-06-06-toto-2026-implementation.md`
- Tasks 7–15 в плане: строки 1072–2743
- Язык общения: русский
- Пользователь: "сначала делаем, потом докручиваем" — MVP важнее идеальной архитектуры
- Next.js версия: 16.2.7 (latest), не 14. App Router идентичен.
- `.env*` в gitignore — `.env.example` добавлен force-track через `git add --force`
- `exactCount` в leaderboard: magic numbers 11/22/32 — привязаны к scoring.ts, нужен комментарий (minor issue, не блокер)

## Starter Prompt for Next Session
> Продолжаем Тото 2026 (футбольный тотализатор ЧМ 2026). Репо: `/Users/nichegosebe/toto-2026/`. Tasks 2–6 (backend) выполнены. Читай `.handoff.md` для полного контекста, затем запусти `superpowers:subagent-driven-development` и продолжи с Task 7 (Layout + Navbar) из плана `docs/superpowers/plans/2026-06-06-toto-2026-implementation.md`.
