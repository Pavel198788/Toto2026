# Handoff — 2026-06-25

## Project
Тото 2026 — футбольный тотализатор ЧМ-2026. Репо: `/Users/nichegosebe/toto-2026/`

## What We Accomplished This Session

1. **Фикс скролла** — горизонтальные ленты (вкладки навигации, фильтры матчей, аналитика) больше не «плывут» по вертикали при свайпе (`overflow-y-hidden` + `touch-pan-x` + `overscroll-x-contain`). **Задеплоено на прод и закоммичено** (commit A).
2. **Возврат в выбранный тур** — со страницы матча кнопка «← К матчам» возвращает в тот тур, откуда зашёл (`?from=<tab>`). **Задеплоено на прод и закоммичено** (commit B). Шрифт кнопки увеличен до `text-sm`, uppercase.
3. **Ручная запись прогноза** — Бутрин Антон, матч Spain–Saudi Arabia, прогноз 3:0 (записан напрямую в Neon как исключение, т.к. не успел до дедлайна).
4. **РАЗДЕЛ PLAY-OFF — реализован, НЕ задеплоен.** Лежит на ветке `worktree-playoff-section` (worktree: `.claude/worktrees/playoff-section`), 4 коммита впереди `main`.

## Play-off — что сделано (ветка worktree-playoff-section)

Спека: `docs/superpowers/specs/2026-06-25-playoff-section-design.md`
План: `docs/superpowers/plans/2026-06-25-playoff-section.md`

- **Task 1** `198c755` — `MatchesList`: поле `winner` + проп `showAdvance` (подсветка победителя жёлтым + стрелка ↑ у завершённых плей-офф матчей).
- **Task 2** `2aff178` — вкладка «Матчи»: новый чип «Плей-офф» (все плей-офф стадии разом) + починен пропавший «1/16» (`ROUND_OF_32`) + `winner` в серилизацию.
- **Task 3** `3b8bbbf` — навигация: вкладка «ПЛЕЙ-ОФФ» вставлена между «ПРОФИЛЬ» и «СЕТКА».
- **Task 4** `42661e9` — новый роут `/playoff`: список плей-офф матчей по стадиям (1/16 → Финал), публичный, ставка/очки только залогиненным, пустое состояние «Плей-офф ещё не начался».

`npm run build` — зелёный после каждой задачи. Поведение подсветки победителя визуально не проверено (в БД пока нет плей-офф матчей — это норма до конца группового этапа).

## Current State / Git

- Worktree `playoff-section` на ветке `worktree-playoff-section`, дерево чистое, 4 коммита впереди `main`.
- `main` содержит фиксы скролла и возврата в тур (задеплоены на прод).
- **Локальные коммиты НЕ запушены в `origin/main`** (пуш через `env -u HTTPS_PROXY -u HTTP_PROXY git push origin main`).

## Pending Tasks (на утро)

1. [ ] **Ревью раздела Play-off** на ветке `worktree-playoff-section`. Можно слить в `main` (ff) и задеплоить: `npx vercel --prod`.
2. [ ] **Сказать «деплой»**, если всё ок — задеплою Play-off на прод (сейчас НЕ задеплоен по договорённости).
3. [ ] **Запушить main в origin** — там 6+ локальных коммитов не в origin (фиксы скролла/тура + при слиянии плей-офф).
4. [ ] Pre-existing: 5 упавших тестов в `__tests__/` (не регрессия, известная проблема).

## Blockers & Open Questions
- Нет критических блокеров. Раздел Play-off ждёт ревью и слова «деплой».
