# Handoff — 2026-06-12 (сессия 17)

## Project
Тото 2026 — футбольный тотализатор ЧМ-2026. Репо: `/Users/nichegosebe/toto-2026/`

## What We Accomplished This Session

- **Починили деплой** — обнаружили 2 Vercel-проекта (`toto-2026` и `toto-group2`): все изменения уходили в `toto-group2`, а пользователи открывали `toto-2026.vercel.app`. Удалили `toto-group2`, `.vercel/project.json` теперь всегда указывает на `toto-2026`.
- **Блок "НАСКОЛЬКО ТЫ УНИКАЛЕН"** — задеплоен в правильный проект, виден у пользователей.
- **События матча** — добавлено поле `events Json?` в модель `Match` (через `prisma db push`). На странице матча отображаются голы, пенальти, красные карточки с именами и минутами в одной карточке вместе с результатом.
- **Автосинк событий** — `sync-matches.ts` при переходе матча в FINISHED автоматически запрашивает события у `sstats.net /games/{id}` и сохраняет в БД.
- **Глобальный live авто-рефреш** — `components/live-refresh.tsx` добавлен в root layout. Каждые 60 секунд во время матча: проверяет `/api/live-scores`, если есть живой матч — вызывает `POST /api/live-sync` (синк today), затем `router.refresh()`. Покрывает все страницы.
- **Данные заполнены** — Mexico vs South Africa (5 событий) и South Korea vs Czechia (3 гола) заполнены в БД через Neon SQL API.

## Current State

Всё задеплоено и работает на `toto-2026.vercel.app`. Репо чистый, uncommitted изменений нет. Следующий матч — **Canada vs Bosnia & Herzegovina** сегодня 22:00 МСК — будет первым тестом авто-рефреша и автосинка событий в бою.

**Git state:**
- Branch: `main`, синхронизирован с `origin/main`
- Uncommitted: ничего
- Last commit: `995c827` (feat: global live auto-refresh)

## Pending Tasks

1. [ ] **Проверить авто-рефреш в бою** — Canada vs Bosnia & Herzegovina сегодня 22:00 МСК. После окончания (~23:55) убедиться что результат + события появились автоматически без ручного деплоя.
2. [ ] **Проверить блок "НАСКОЛЬКО ТЫ УНИКАЛЕН"** у Хлебникова Павла — после деплоя в правильный проект он должен появиться в профиле. Условие: `uniqueness.total > 0` (нужно ≥2 завершённых матча с перекрёстными прогнозами).
3. [ ] **Подтвердить клики на матчи** — пользователь ещё не подтвердил что переход на `/matches/{id}` работает.
4. [ ] **Проверить начисление очков** — зайти на `/standings` или `/grid`, убедиться что очки за 2+ завершённых матча начислены верно.
5. [ ] **Vercel cron лимиты** — Hobby plan: максимум 2 активных cron из 7 в `vercel.json`. Сейчас их 8 — нужно проверить в Vercel Dashboard какие реально работают.

## Blockers & Open Questions

- Vercel Hobby допускает только 2 cron-задания — при 8 в `vercel.json` часть не выполняется. Но с живым авто-рефрешем cron теперь резервный механизм, не основной.
- `prisma db push` применялся вместо `prisma migrate dev` из-за drift — migration history не синхронизирована с prod DB. Не критично пока, но при следующей миграции нужно снова использовать `db push`.

## Key Decisions Made

- **Два Vercel-проекта**: `toto-group2` удалён, единственный проект — `toto-2026`. `.vercel/project.json` зафиксирован на `prj_rZU6YsuaG7IGafNPIDwUQxtpyhIW`.
- **Авто-рефреш через layout**: `<LiveRefresh />` в `app/layout.tsx` — покрывает все страницы без дублирования кода.
- **События только для значимых**: жёлтые карточки и замены не показываются — только голы, пенальти, красные. Пользователь согласился.
- **`prisma db push` вместо migrate**: из-за drift схемы. Новые миграции тоже делать через `db push`.

## Context to Remember

- Vercel: `toto-2026.vercel.app`, Hobby план, проект `toto-2026` (ID: `prj_rZU6YsuaG7IGafNPIDwUQxtpyhIW`)
- DB: Neon PostgreSQL `ep-red-scene-aqh8s2tq-pooler.c-8.us-east-1.aws.neon.tech`, password: `npg_VbaFPOI3iT4G`
- Neon HTTP SQL API: `POST https://ep-red-scene-aqh8s2tq-pooler.c-8.us-east-1.aws.neon.tech/sql` с заголовком `Neon-Connection-String`
- Admin: `admin@toto2026.ru` / `admin123` (нет прогнозов — не использовать для проверки блоков профиля!)
- GitHub push: `env -u HTTPS_PROXY -u HTTP_PROXY git push origin main`
- Shell cwd сбрасывается на `/Users/nichegosebe/cruise-guide` — использовать абсолютные пути
- 14 участников, 2+ завершённых матча на 2026-06-12
- sstats.net API: `/games/list?LeagueId=1&Year=2026` (список), `/games/{id}` (детали + события)
- TypeScript ошибки в `.next/dev/types/` — pre-existing, на production build не влияют
- Деплой: `cd /Users/nichegosebe/toto-2026 && npx vercel --prod`

## Starter Prompt for Next Session

> Продолжаем Тото 2026. Репо: `/Users/nichegosebe/toto-2026/`. Читай `.handoff.md`. Первое: проверить отработал ли авто-рефреш на матче Canada vs Bosnia & Herzegovina (22:00 МСК 12 июня) — появились ли результат и события автоматически. Если нет — диагностировать `/api/live-sync` и `components/live-refresh.tsx`.
